DEFINE TABLE property;

DEFINE FIELD address    ON property TYPE string;
DEFINE FIELD price      ON property TYPE float;
DEFINE FIELD type       ON property TYPE string;
DEFINE FIELD bedrooms   ON property TYPE int;
DEFINE FIELD bathrooms  ON property TYPE int;
DEFINE FIELD active     ON property TYPE bool;
DEFINE FIELD created    ON property TYPE datetime;
