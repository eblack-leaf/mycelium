DEFINE TABLE customer;

DEFINE FIELD name     ON customer TYPE string;
DEFINE FIELD email    ON customer TYPE string;
DEFINE FIELD phone    ON customer TYPE string;
DEFINE FIELD created  ON customer TYPE datetime;
