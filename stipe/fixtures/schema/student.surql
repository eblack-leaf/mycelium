DEFINE TABLE student;

DEFINE FIELD name      ON student TYPE string;
DEFINE FIELD email     ON student TYPE string;
DEFINE FIELD gpa       ON student TYPE float;
DEFINE FIELD enrolled  ON student TYPE bool;
DEFINE FIELD created   ON student TYPE datetime;
