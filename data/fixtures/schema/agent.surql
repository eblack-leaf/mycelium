DEFINE TABLE agent;

DEFINE FIELD name    ON agent TYPE string;
DEFINE FIELD email   ON agent TYPE string;
DEFINE FIELD phone   ON agent TYPE string;
DEFINE FIELD rating  ON agent TYPE float;
DEFINE FIELD active  ON agent TYPE bool;
