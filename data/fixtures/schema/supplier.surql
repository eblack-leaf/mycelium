DEFINE TABLE supplier;

DEFINE FIELD name     ON supplier TYPE string;
DEFINE FIELD email    ON supplier TYPE string;
DEFINE FIELD country  ON supplier TYPE string;
DEFINE FIELD rating   ON supplier TYPE float;
DEFINE FIELD active   ON supplier TYPE bool;
DEFINE FIELD created  ON supplier TYPE datetime;
