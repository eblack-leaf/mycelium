DEFINE TABLE warehouse;

DEFINE FIELD name      ON warehouse TYPE string;
DEFINE FIELD location  ON warehouse TYPE string;
DEFINE FIELD capacity  ON warehouse TYPE int;
DEFINE FIELD active    ON warehouse TYPE bool;
